import { ThemeToggleButton } from '../ThemeToggleButton/ThemeToggleButton';
import { Container } from './styles';

export const Header = () => {
  return (
    <Container>
      <ThemeToggleButton />
    </Container>
  );
};
